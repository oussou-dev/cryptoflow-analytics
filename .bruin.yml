default_environment: default

environments:
  default:
    connections:
      duckdb:
        - name: "duckdb-default"
          path: "md:cryptoflow_analytics"
          token: "{{ env 'MOTHERDUCK_TOKEN' }}"
