default_environment: default

environments:
  default:
    connections:
      motherduck:
        - name: "motherduck-default"
          database: "cryptoflow"
          token: "{{ env 'MOTHERDUCK_TOKEN' }}"
