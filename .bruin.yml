default_environment: default

environments:
  default:
    connections:
      google_cloud_platform:
        - name: "bigquery-default"
          project_id: "cryptoflow-p"
          service_account_file: "gcp-key.json"
